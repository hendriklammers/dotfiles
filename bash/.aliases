alias reload='source ~/.bashrc'
alias ea='vi ~/dotfiles/bash/.aliases && reload'
alias func='vi ~/dotfiles/bash/.functions && reload'
alias cfg='vi ~/dotfiles/bash/.config && reload'

# Terminal
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias la='ls -A -l -G' # List all
alias l.='ls -d .[^.]*' # List hidden
alias lsd='ls -l | grep "^d"' # List directories
alias cl='clear' #clear screen

# Recursively delete `.DS_Store` files
alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"

# Finder
alias o='open .'
alias showhidden="defaults write com.apple.Finder AppleShowAllFiles -bool true && killall Finder"
alias hidehidden="defaults write com.apple.Finder AppleShowAllFiles -bool false && killall Finder"

# Processes
alias tu='top -o cpu' # cpu
alias tm='top -o vsize' # memory

# IP address
alias ip="dig +short myip.opendns.com @resolver1.opendns.com"
alias localip="ipconfig getifaddr en1"
alias ips="ifconfig -a | perl -nle'/(\d+\.\d+\.\d+\.\d+)/ && print $1'"

# Edit settings
alias editssh='mate -a ~/.ssh/config'

# Git
alias gb='git branch'
alias ga='git add'
alias gc='git commit'
alias gco='git checkout'
alias gd='git diff | mate'
alias gdm='git diff master'
alias gl='git log'
alias gla='git log --all --oneline --graph --decorate'
alias gnp="git-notpushed"
alias gp='git push'
alias gs='git status -sb'
alias gt='git tag'
alias gm='git merge --no-ff'

# Merge PDF files
# Usage: `mergepdf -o output.pdf input{1,2,3}.pdf`
alias mergepdf='/System/Library/Automator/Combine\ PDF\ Pages.action/Contents/Resources/join.py'

# Shortcuts
alias dot='cd ~/dotfiles'
alias dl='cd ~/Downloads'
alias code='cd ~/code'
alias hendrik='cd ~/Dropbox/website_2013/06_development'
alias vimprac='cd ~/code/practise/vim'
